<div class="hero-inner container">
    <div class="photo-frame">
        <img src="{{profile_img}}" alt="{{name}}" />
    </div>
    <p class="kicker">{{title}}</p>
    <h1>{{name}}</h1>
    <p class="tagline">Desarrollando soluciones con tecnología y pasión.</p>
    <div class="hero-bio"><p>{{bio}}</p></div>
</div>
